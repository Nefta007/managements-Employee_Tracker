const connection = require("../config/connection");
const cTable = require('console.table');
//const inquirer = require('inquirer');

function role_lib(userDepartment){
    console.log(userDepartment);
}

module.exports = role_lib;